#include <iostream>
using namespace std;
int main()
{
    int length, width;
    cin >> length >> width;
    cout << "Circum: " << (length+width)*2 << endl << "Area: " << length*width << endl;
    return 0;
}
